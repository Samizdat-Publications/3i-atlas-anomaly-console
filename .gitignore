# Raw JPL Horizons API responses — regenerate with tools/fetch_ephemeris.py
data/raw/

# Superseded by git history (kept locally so the offline file keeps a rollback trail)
_Archive (old versions)/

# Wrangler local build cache (recreated on every deploy)
.wrangler/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
*.swp
.vscode/
.idea/
__pycache__/
*.pyc

# Transcripts ARE tracked (owner's decision, 2026-08-23) so a research notebook
# can ingest them by URL. Attribution and provenance: data/transcripts/README.md.
# Only the fetcher's scratch space is ignored.
data/transcripts/.tmp/
